export const PRIVATE_ROUTES = {
    layout: '/',
    jobs: '/jobs',
    updateUser: '/update-user'
};

export const PUBLIC_ROUTES = {
    layout: '/',
    login: '/login',
    register: '/register',
};
